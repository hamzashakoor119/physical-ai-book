[phases.setup]
nixPkgs = ["nodejs_22", "python3", "python3Packages.pip"]

[phases.install]
cmds = ["npm ci", "cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
